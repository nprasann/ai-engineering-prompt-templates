# AGENTS.md

## Project Overview
`<PROJECT_NAME>` is a `<TECH_STACK>` project.

Repository: <REPO_URL>
Local path: <LOCAL_PATH>
Current task focus: <TASK>
Constraints: <CONSTRAINTS>

## Repository Layout
- `src/`: Source code.
- `tests/`: Automated tests.
- `docs/`: Project documentation.

## Working Rules
- Inspect relevant files before editing.
- Start with `<FILES_TO_INSPECT>` when provided.
- Keep changes focused on `<TASK>`.
- Prefer minimal, reviewable patches.
- Do not introduce secrets, credentials, production data, or private details.

## Commands
- Install: `<COMMAND_TO_INSTALL>`
- Run: `<COMMAND_TO_RUN>`
- Test: `<COMMAND_TO_TEST>`
- Lint: `<COMMAND_TO_LINT>`

## Session Summary
At the end of work, summarize files changed, tests run, risks, and follow-ups.
Use `<OUTPUT_FORMAT>` when a specific format is requested.
